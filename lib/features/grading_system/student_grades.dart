import 'package:flutter/material.dart';

class StudentGradesPage extends StatelessWidget {
  const StudentGradesPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Student Grade Page'),
      ),
    );
  }
}
