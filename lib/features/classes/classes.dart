import 'package:flutter/material.dart';

class HandledClasses extends StatelessWidget {
  const HandledClasses({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Classes Page')),
    );
  }
}
